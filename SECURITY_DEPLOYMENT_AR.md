# متطلبات النشر الآمن

هذه النسخة تفشل بصورة آمنة ولا تعرض بيانات المرضى إذا كانت حماية الدخول غير مكتملة.

## متغيرات Netlify الإلزامية

أضف القيم التالية في **Project configuration → Environment variables**:

- `AUTH_ENABLED` = `true`
- `AUTH_SESSION_SECRET` = قيمة عشوائية سرية لا تقل عن 32 حرفًا
- `AUTH_BOOTSTRAP_USERNAME` = اسم مستخدم الإدارة الرئيسي
- `AUTH_BOOTSTRAP_PASSWORD` = كلمة مرور قوية لا تقل عن 12 حرفًا
- `AUTH_BOOTSTRAP_DISPLAY_NAME` = الاسم الظاهر للمستخدم الرئيسي
- `APP_ORIGIN` = `https://bestcaredentalclinicsdash.netlify.app`

لا تضع أي قيمة سرية داخل ملفات GitHub.

## متغيرات اختيارية

- البريد: `RESEND_API_KEY` و`AUTH_EMAIL_FROM`
- الرسائل النصية: متغيرات مزود SMS المعتمد
- الإشعارات الخارجية: `VAPID_PUBLIC_KEY` و`VAPID_PRIVATE_KEY` و`VAPID_SUBJECT`

## اختبار ما قبل الإنتاج

1. سجّل الدخول بحساب الإدارة.
2. افتح حساب طبيب وتأكد أنه لا يستطيع فتح صفحة الإدارة أو عيادة أخرى.
3. افتح التاريخ نفسه على جهازين وتأكد من ظهور التحديث على الجهاز الآخر.
4. اختبر تسجيل الخروج، ثم تأكد أن الرجوع بالمتصفح لا يعرض بيانات المرضى.
5. اختبر انتهاء الجلسة بعد الخمول، والتنبيه الخارجي، واعتماد خطة علاجية تجريبية.
6. بعد نجاح المعاينة فقط، ادمج الفرع في `main` واترك Netlify ينشر مرة واحدة.
