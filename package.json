{
  "name": "best-care-dashboard-v4",
  "private": true,
  "version": "4.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check-syntax.mjs && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^10.0.0",
    "web-push": "^3.6.7"
  }
}
